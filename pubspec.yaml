name: ct484_project
description: "Project for CT484."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  google_fonts: ^6.2.0
  image_picker: ^1.1.2
  photo_view: ^0.15.0
  flutter_svg: ^2.0.5
  smooth_page_indicator: ^1.2.0+3
  intl: ^0.19.0
  provider: ^6.1.2
  dropdown_search: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/svg/
